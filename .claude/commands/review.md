Scan the codebase for issues.

1. Run `npm run typecheck` and report any TypeScript errors
2. Review components for accessibility gaps (missing alt text, aria labels, keyboard navigation)
3. Flag any code quality concerns (unused imports, inconsistent patterns, missing error boundaries)
4. Summarize findings with file paths and line numbers
