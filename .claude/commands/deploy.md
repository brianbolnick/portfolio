Build, commit staged changes, and push to remote.

1. Run `npm run build` to verify the build succeeds
2. Stage all changed files with `git add -A`
3. Ask me for a commit message, then commit
4. Push to the current remote branch
